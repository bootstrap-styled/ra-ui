
const __rest = (this && this.__rest) || function (s, e) {
  const t = {};
  for (var p in s) { if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p]; }
  if (s != null && typeof Object.getOwnPropertySymbols === 'function') {
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
    }
  }
  return t;
};
Object.defineProperty(exports, '__esModule', { value: true });
exports.default = (function (_a) {
  const { alwaysOn } = _a;
  const { basePath } = _a;
  const { component } = _a;
  const { defaultValue } = _a;
  const { formClassName } = _a;
  const { initializeForm } = _a;
  const { input } = _a;
  const { isRequired } = _a;
  const { label } = _a;
  const { limitChoicesToValue } = _a;
  const { locale } = _a;
  const { meta } = _a;
  const { options } = _a;
  const { optionText } = _a;
  const { optionValue } = _a;
  const { record } = _a;
  const { resource } = _a;
  const { allowEmpty } = _a;
  const { source } = _a;
  const { textAlign } = _a;
  const { translate } = _a;
  const { translateChoice } = _a;
  const rest = __rest(_a, ['alwaysOn', 'basePath', 'component', 'defaultValue', 'formClassName', 'initializeForm', 'input', 'isRequired', 'label', 'limitChoicesToValue', 'locale', 'meta', 'options', 'optionText', 'optionValue', 'record', 'resource', 'allowEmpty', 'source', 'textAlign', 'translate', 'translateChoice']);
  return rest;
});
